#find addition of digit of an integer:

def sn(num):
    sum = 0
    while (num != 0):
       
        sum = sum + (num % 10)
        num = num//10
       
    return sum

    # sum = 0
    # while (num != 0):

    #     sum = sum + num % 10
    #     num//10
    
    # return sum


num = int ( input ("Enetr the number : "))
print(sn(num))